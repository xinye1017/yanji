# 「研迹」技术栈与架构

> **项目名称**：研迹（Yanji）
> **产品定位**：个人专属考研学习记录与阶段复盘 Android 客户端（Local-First 离线优先）
> **设计风格**：蓝白低饱和、大圆角、克制留白、Floating Glass-inspired 悬浮导航栏
> **核心 IP**：卷卷
>
> **文档状态**：✅ 已与源码同步（核对日期 2026-09-11，对应数据库 v9）。
> 每节标注 `Status`，区分「已实现」与「规划中」，避免把设计目标写成当前事实。
>
> **仓库**：`github.com/xinye1017/yanji`（私密，分支 `main`）

---

## 一、核心语言与构建体系

| 组件 / 规范 | 版本 / 配置 | 说明 |
| :--- | :--- | :--- |
| **编程语言** | **Kotlin 2.3.20** | 纯 Kotlin + Coroutines + Flow |
| **JVM Toolchain** | **Java 17** | `sourceCompatibility/targetCompatibility = 17` |
| **构建系统** | **Gradle Wrapper 9.1.0 + AGP 9.0.1** | wrapper 实际分发版本见 `gradle/wrapper/gradle-wrapper.properties` |
| **版本治理** | Gradle Version Catalog | `gradle/libs.versions.toml` |
| **注解处理器** | **KSP 2.3.11** | 为 Room 实体与 DAO 生成代码 |
| **系统支持** | **minSdk 24 / targetSdk 36 / compileSdk 36** | Android 7.0 — Android 16 |
| **release 构建** | **R8 开启**（`isMinifyEnabled` + `isShrinkResources`） | 规则见 `app/proguard-rules.pro`；实测 release APK 约 2.8 MB |

> ⚠️ 构建机注意：`gradle.properties` 里的 `-Dfile.encoding=GBK` 是**中文 Windows 路径**
> 的 workaround（Gradle test worker 的 classpath argfile 编码问题），在 UTF-8 的 CI 上
> 应改为 UTF-8，不要把它当成通用构建契约。详见 `开发手册/架构与能力状态.md`。

---

## 二、UI 与交互架构

- **UI 引擎**：Jetpack Compose（Compose BOM `2026.03.01`），无 XML / Fragment。
- **设计规范**：Material Design 3。
- **页面路由**：**自定义状态机导航**（`Navigation.kt` 的 `currentTab` + `mutableStateListOf<YanjiSubScreen>`）。
  - `Status`：**已实现的是自定义导航**。项目虽引入了 `androidx.navigation3`（runtime/ui/viewmodel）
    三个依赖，但 `NavigationKeys.kt` 里只声明了一个 `Main : NavKey`，实际导航完全没有走 Navigation 3。
  - `Status: Planned` — 要么真正迁移到 Navigation 3 typed back stack，要么移除依赖并保持现状。
- **图标体系**：Phosphor Icons `1.0.0` + 部分 Material Icons Extended。
- **特色交互**：Floating Navigation 悬浮胶囊导航栏（`AppContentInsets.BottomBarPadding`）、
  `FlowRow` 自适应折行、7 天柱状图点击直通当日明细。
- **Flow 采集**：全部使用 `collectAsStateWithLifecycle()`（见 `ui/` 下 17 个文件），
  UI 不可见时停止采集，避免后台无效开销。

---

## 三、数据持久化体系

- **引擎**：AndroidX Room **2.8.4** + SQLite。
- **当前 schema 版本：v9**（`YanjiDatabase`，`exportSchema = true`）。
- **迁移原则**：
  - 严格手写 `Migration`，**绝不使用 `fallbackToDestructiveMigration()`** —— 迁移路径缺失时
    让 Room 直接抛异常暴露问题，而不是静默删除真实数据。
  - 迁移统一覆写 `migrate(connection: SQLiteConnection)`（Room 2.8 的 KMP 变体在 JVM 上
    只有这一个签名；只覆写 `SupportSQLiteDatabase` 版本会导致迁移在带 SQLiteDriver 的环境里被跳过）。
  - 不使用 `ALTER TABLE ... DROP COLUMN`（需要 SQLite ≥ 3.35，minSdk 24 真机不满足），
    统一走「建新表 → `INSERT SELECT` → `DROP` → `RENAME`」。
- **数据表与职责**：

| 表 | 职责 | 索引 |
| :--- | :--- | :--- |
| `focus_sessions` | 单次专注会话（科目、起止时间、有效秒数、暂停次数/时长、模式、备注、状态） | `startTime` |
| `exam_sessions` | 全真模考会话（计划/实际用时、得分、满分、复盘） | `startTime` |
| `journal_entries` | 每日复盘（心态/精力/满意度打分、正文、次日计划、标签） | `date` |
| `user_settings` | 目标院校专业、每日目标、有效时长阈值、**AI 非敏感配置**（不含 API Key） | — |
| `chat_sessions` / `chat_messages` | 卷卷多会话对话存档 | `sessionId`、`timestamp` |
| `check_ins` / `unlocked_achievements` | 打卡与成就解锁 | — |
| `quick_start_presets` | 首页快捷操作（内置模板 + 自定义组合） | `sortOrder` |

> 备份文件格式有自己的版本号（`YanjiBackup.schemaVersion`，当前 v1），与数据库版本**解耦**：
> 前者服务"文件在设备间流转"，后者服务"已安装应用内的数据升级"。
> 导出内容**结构上不含 AI API Key**（`UserSettingsBackup` 里没有这个字段）。

> **注意**：AI API Key **不存在于 Room**，也不存在于 SharedPreferences。
> 详见「六、安全与隐私」。

---

## 四、数据流与架构分层

### 当前架构（Status: Implemented）

```text
[Compose Screens]
  ├── 直接持有 YanjiRepository.getInstance() （仍待改造）
  └── collectAsStateWithLifecycle() 观察 StateFlow
        │
        ▼
[YanjiRepository]  ← God Object（1700 → 1249 行，AI 与聊天已剥离）
  数据库绑定 / 设置 / 成就聚合 / 统计口径
        │
        ├──► Room DAOs + Entities
        ├──► AiClient → 第三方 AI API
        ├──► ChatStore → 聊天会话与消息
        └──► BackupCodec / BackupTransfer → 备份文件

[FocusTimerService]  只负责时间事实（单调时钟 + 通知）
        │
        ▼
[ActiveSessionCoordinator]  ← 活动会话的业务归属者
        │
        ▼
[YanjiRepository] 落库（不依赖任何 Compose 页面是否在组合）
```

### 目标架构（Status: Planned，阶段三）

```text
Compose UI → Feature ViewModel → UseCase / Repository → Room / Service
                                 ├── StudyRepository
                                 ├── JournalRepository
                                 ├── ChatRepository
                                 ├── SettingsRepository
                                 ├── TimerRepository
                                 └── AiClient（网络 + 错误映射）
```

---

## 五、计时体系（Status: Implemented）

- **纯 Kotlin 状态机**：`data/timer/TimerMachine.kt` + `TimerSnapshot`，
  时钟通过 `MonotonicClock` 注入，可在纯 JVM 单测里用假时钟驱动。
- **计时事实 = 单调时钟差值**。`SystemClock.elapsedRealtime()`（单调、含 deep sleep）
  才是「专注了多久」的正确基准；每秒 tick 只刷新展示值，不再 `elapsed++`。
  挂钟时间（`startedAtEpochMs` / `endEpochMs`）只用于用户查看日期。
- **状态迁移**：`IDLE → RUNNING ⇄ PAUSED → COMPLETED / CANCELLED`。
- **语义统一定义**：
  - 倒计时归零 / 用户主动「结束」 → `COMPLETED`，按实际时长落库；
  - 用户「放弃 / 退出」 → `CANCELLED`，不产生记录；
  - 专注不足 60 秒不记录（与 UI 提示同一条规则）。
- **互斥不变量**：同一时刻只允许一个活动会话，专注与模考互斥，由
  `ActiveSessionCoordinator.begin()` 统一拒绝。
- **完成不依赖 UI**：落库出口在 `ActiveSessionCoordinator`，前台 Service 或任何页面调用都一样。
- **WakeLock**：与计时生命周期完全绑定，完成瞬间立即释放。
- **前台 Service**：`specialUse` 类型，`START_NOT_STICKY`（避免 null intent 重启出假状态）。
  通知渠道锁屏可见性为 `PRIVATE`。

---

## 六、安全与隐私（Status: Implemented）

- **凭据存储**：AI API Key 由 `KeystoreSecretStore` 用 Android Keystore 的 AES-GCM 加密后
  写入 `Context.getNoBackupFilesDir()`。该目录**系统保证永不参与备份/设备迁移**，
  Keystore 密钥也永不离开设备。
  - 为什么不用 SharedPreferences：SharedPreferences 默认属于 Android Auto Backup 范围。
- **备份策略**：`AndroidManifest.xml` 显式引用 `@xml/dataExtractionRules` 与
  `@xml/fullBackupContent`，两者都只 `include domain="database"` ——
  即**只备份学习数据，凭据天然排除**。
- **明文流量**：全局关闭。`res/xml/network_security_config.xml` 默认
  `cleartextTrafficPermitted="false"`，仅放行 `127.0.0.1 / localhost / 10.0.2.2`。
  局域网 HTTP 服务请用 HTTPS 反向代理，或 `adb reverse tcp:<port> tcp:<port>` 后填回环地址。
  `CleartextPolicy` 会在 AI 配置界面提前给出可读的阻断说明。
- **日志**：不记录 API Key，不把第三方接口的完整错误正文写入 Logcat（只记状态码与长度）。
- **AI 数据边界**：命中 AI 请求时会把学习统计与日记上下文发给用户自配置的 Provider；
  `JuanjuanPromptTest` 有断言保证运行时上下文里**不包含 API Key**。

---

## 七、AI 与网络

- **网络底座**：原生 `java.net.HttpURLConnection`（无三方 HTTP 依赖）。
- **协议**：兼容 OpenAI 标准接口（默认预设 DeepSeek，可自由配置 Base URL / 模型）。
  支持探测 `/v1/models` 拉取可用模型列表。
- **AI 对话**：答疑、状态疏导、按首轮问答生成 ≤10 字会话标题；无 Key 时走本地知识库回复。
- `Status: Planned` — **当前主路径读取完整 JSON response，并非真正的流式输出**。
  如需要流式，需改为 `HttpURLConnection` 分块读取或引入 SSE 支持。

---

## 八、测试与质量保障

| 层次 | 现状 |
| :--- | :--- |
| 单元测试 | JUnit 4.13.2 + `kotlinx-coroutines-test` 1.10.2，**49 个用例，纯 JVM，全绿** |
| 数据库迁移 | `YanjiMigrationTest`（6）：1→9 全链 + 7→8 单跳，用 Room 导出的真实 schema JSON 做结构校验 |
| 计时状态机 | `TimerMachineTest`（9）：暂停区间、倒计时边界、结束后冻结、时钟漂移 |
| 备份编解码 | `BackupCodecTest`（10）：往返、版本拒绝、格式容错、**导出不含 API Key** |
| AI 协议层 | `AiProtocolTest`（12）：端点拼接、三种 `/models` 结构解析、内容提取、错误文案 |
| AI 协议 | `JuanjuanPromptTest`（4，含「上下文不得包含 API Key」） |
| 统计与学科 | `StudyDiagnosticsTest`（2） |
| 响应解析 | `JuanjuanResponseParserTest`（6） |
| 插桩测试 | **`Status: Implemented`** —— 9 个用例，真机 / 模拟器运行：`AppInitializerInstrumentedTest`（4，含"删空后重启仍为空"）、`BackupTransferInstrumentedTest`（5，导出/导入整表替换往返） |
| 覆盖率 | `Status: Planned` —— 未接入 Kover/JaCoCo |
| 静态检查 | `Status: Planned` —— 未配置 Detekt/Ktlint gate |
| CI | `Status: Implemented` —— `.github/workflows/android-ci.yml`：单测 → debug → release(R8)；另有一个 API 34 模拟器任务跑插桩测试 |
