# 2026-09-14 · Yanji v1.0.0 低风险收尾（测试同步 / Design Token 清理 / 品牌统一）

## 本轮完成

按「Yanji v1.0.0 低风险收尾、测试同步与 Design Token 清理任务」执行，改动如下：

1. **修好 `androidTest` 源集编译失败**（这是 CI `connectedDebugAndroidTest` 真正的第一道红灯）：
   `JuanjuanChatScreenInstrumentedTest` 调 `ComposerBar` 时漏传新增的必填参数 `isAiConfigured`，
   `:app:compileDebugAndroidTestKotlin` 直接 FAILED，根本没跑到断言。
2. **三组插桩测试契约重写**：
   - Chat：一个 test method 内两次 `setContent` 拆成两个独立测试；过期文案对齐当前生产 UI；
     新增 `ChatInputTestTag` / `ChatSendButtonTestTag` / `UserMessageBubbleTestTag` /
     `JuanjuanMessageBubbleTestTag`，改成 tag 定位 + 行为断言（输入→发送）。
   - Journal：删掉「今日心境 / 今日随笔与复盘」这类过期标题断言，改为 tag 定位的行为测试
     （渲染、mood 选中态、写作区输入、空内容保存被拦、有内容保存回调成功）。
   - LiveActivity：把「系统能力」与「通知 runtime 可用性」拆成两条不变量。
3. **AchievementsScreen 稀有度颜色迁移**到 `theme/Color.kt` 的 `Achievement*` token（16 个），
   `ui/` 下 `Color(0x...)` 归零。
4. **品牌统一**：README 里 7 处「研己」→「研迹」。
5. **DESIGN.md 圆角规范与 `theme/Radius.kt` 对齐**（button 12 / input 16，原写 20，从未落地）。
6. 新增 JVM 单测 `LiveActivityCapabilityTest`（6 个用例）钉死 `tier` 契约。

## 验证结果（隔离 worktree @ HEAD + 本轮改动，API 35 x86_64 emulator）

- `:app:testDebugUnitTest` → 19 classes / **132 tests / 0 failures / 0 skipped**
- `:app:lintDebug` → **0 errors**, 38 warnings, 3 hints
- `:app:assembleDebug` → OK
- `:app:assembleRelease` → OK（R8 + resource shrink）
- `:app:connectedDebugAndroidTest` → **31 tests / 0 failures / 0 skipped**

## 踩坑与新事实（重要）

- **本机模拟器镜像坏了**：`system-images/android-35/google_apis/x86_64` 里 **`system.img` 与
  `vendor.img` 缺失**（只有 kernel/ramdisk），emulator 报
  `No initial system image for this configuration!`，AVD `DiaryPhone` 起不来。
  `sdkmanager --install` 会因目录里还留着 `package.xml` 而**认为已安装、不重下**；
  必须先把该目录**移出 SDK 根**（放 `Sdk/_quarantine_*` 仍会被扫到，要放到 `C:/Users/dex/` 下）
  再重下（约 3.5 GB）。修复后 WHPX 加速可用，API 35 模拟器正常。
- **`git worktree remove` 在 Windows 上会因 Gradle build 目录长路径失败**
  （`failed to delete ...: Filename too long`），但**worktree 注册已被摘掉**；
  残留目录用 python `shutil.rmtree` 加 `\\?\` 前缀**也不行**，得用 PowerShell
  `Remove-Item -Recurse -Force`。本次残留 `build/verify-wt`（215 MB）未清干净。
- **并发编辑风险（本轮最大教训）**：会话进行中有**另一个进程在持续改写整个仓库**
  （成就系统 V2 重构，每 7~15 秒改一个文件：theme → components → navigation → screens），
  导致工作区反复不可编译：
  - `MainActivity.kt` 被删掉 `override fun onCreate` 与 `requestPermissionLauncher`，
    函数体裸露在类体里 → 语法错误；
  - `AchievementsScreen.kt` / `HomeScreen.kt` 删了 `androidx.compose.material.icons.*` 导入
    但 `Icons.Default.X` 用法还在 → `Unresolved reference 'Icons'`。
  → **结论：在同一仓库上并行跑两个改造任务会让验证彻底不可靠。**
    今后遇到这种情况，一律用 `git worktree add` 拉隔离副本验证，不要在活动工作区里反复构建。
- 修复动作与备份：被外部改坏的 `MainActivity.kt` 备份在 `build/wip-backups/`，
  工作区里已临时恢复为 HEAD 版本（可编译）；验证证据在 `build/verification/`。

## 新增约定（已写进 MEMORY.md）

- 插桩测试一律用导出的 testTag 常量定位，不再断言中文整句文案。
- `JournalEditorScreen` 导出 `object JournalEditorTags`；`JournalViewModel.saveJournal` 是 `open`，
  作为「插桩测试不落库」的注入接缝。
- 品牌名唯一正确答案是**研迹**（`strings.xml` 的 `app_name`、DESIGN.md、UI、备份文案、AI prompt 全部一致）。


---

# 第三轮：暗色模式重构（按 design_dark.md）+ 设置页主题切换 —— **被并发进程回滚，已暂停**

## 关键发现（本任务真正的难点）

`ui/` 下 **719 处静态亮色 token** vs **157 处 `MaterialTheme.colorScheme`**。
`YanjiPrimary` 这类是顶层 `val`（编译期常量），永远返回亮色值
⇒ **暗色模式目前对绝大多数界面根本不生效**，切暗色只是「深底 + 亮色字块」。
所以「只改 Color.kt 暗色值 + 加开关」在用户眼里就是没做完。

## 已完成（后被整体回滚）

1. `theme/Color.kt` 暗色区块按 `design_dark.md` 全量重写（含半透明容器 ARGB 常量）。
2. 新建 `theme/ExtraColors.kt`：M3 装不下的 5 个角色（textTertiary / warning / warningSoft /
   lavenderDeep / surfaceBlue）+ `yanjiSeriesColor()` 学科序列色翻译。
3. `theme/Theme.kt`：`YanjiThemeMode` 枚举 + `LocalYanjiDarkTheme` + `yanjiThemeColor()`，
   `DarkColorScheme` 补 `surfaceDim/Bright/Container*` 表达「明度递进层级」。
4. 数据层：`UserSettings.themeMode`（String，宽松解析）、Entity 双向映射、
   **Room v11→v12**（`ALTER TABLE ... ADD COLUMN themeMode TEXT NOT NULL DEFAULT 'SYSTEM'`）、
   `12.json`、`UserSettingsBackup.themeMode`（带默认值以兼容旧备份）、`YanjiMigrationTest` 两个用例。
5. **ui/ 主题化 sweep：700 处 / 42 文件**（亮色下逐值相等 ⇒ 等值替换 ⇒ 零视觉变化）。
6. 12 处非 Composable 上下文修复（Canvas 颜色上提、顶层 val 改 @Composable getter、
   纯函数加注解、枚举 dotColor 改 @Composable 函数、remember lambda 内取色外提）。
7. 编译只剩 1 个错误，且属并发任务（`YanjiRepository.presetStore`）。

## 被回滚

23:26 复检：`HEAD` 仍是 `2ce92a2`（未提交），但上述**全部改动消失**
（静态 token 从 0 退回 722，`ExtraColors.kt` 被删，DB 版本退回 11，`12.json` 消失）。
同时观察到并发进程 staged 删除 `data/preset/QuickStartPresetStore.kt` + `PresetOrderLogic.kt`，
改写 `Navigation.kt` / `AppInitializer.kt` / `HomeViewModel.kt` / `FocusViewModel.kt` / `theme/Theme.kt`。
⇒ **该进程不只是改写文件，而是会重置工作树。**

## 处置

用户选择「等对方跑完再做」。成果已固化为可一键重做的脚本，**全在 `build/dark-mode-work/`**：

- `DARK_MODE_PLAN.md` —— 设计、完整色值映射表、重做步骤、尚未脚本化的收尾项
- `ExtraColors.kt` —— 新文件原文
- `_dark_palette.kt` —— 暗色区块原文
- `reapply.py` —— `--step=core|sweep|migrationtest`
- `theme_sweep.py` / `fix_composable_context.py` —— 替换与修复脚本（带断言）

⚠️ **教训**：在活动工作区做「跨 40+ 文件的大批量改动」前，必须先确认工作区归自己独占；
否则一次 `reset --hard` 就会全部蒸发。下次这类任务直接开 `git worktree`。


---

# 第三轮（续）：暗色模式重构完成

用户在并发任务结束后确认「可以了」，按 `build/dark-mode-work/` 里固化的脚本一键重做并完成。

## 落地内容

1. **调色板**：`theme/Color.kt` 暗色区块按 `design_dark.md` 全量重写（含半透明 ARGB 容器），
   新增 `YanjiDarkSurfaceFloating` / `YanjiDarkTrack` / `YanjiDarkPrimaryGlow` /
   `YanjiDarkPrimaryEdge` / `YanjiDarkDockPanel` / `YanjiDarkDockPill` / `Subject*Dark`。
2. **新增 `theme/ExtraColors.kt`**：M3 装不下的 5 个角色（`textTertiary`/`warning`/`warningSoft`/
   `lavenderDeep`/`surfaceBlue`）做成 `YanjiExtraColors` + `YanjiColors` 访问器；
   以及 `yanjiSeriesColor()` / `yanjiSeriesToken()`（学科序列色按主题翻译）。
3. **`theme/Theme.kt`**：`YanjiThemeMode`（SYSTEM/LIGHT/DARK）+ `LocalYanjiDarkTheme` +
   `yanjiThemeColor()` / `yanjiIsDarkTheme()` / `resolveDarkTheme()`；
   `DarkColorScheme` 补 `surfaceDim/Bright/Container*`（明度递进表达海拔）。
   注意：对方提交曾把默认值改成 `darkTheme = false`（等于关掉系统暗色），已恢复为跟随设置解析。
4. **数据层**：`UserSettings.themeMode`（String + 宽松解析）、Entity 双向映射、
   **Room v11→v12**（`ADD COLUMN themeMode TEXT NOT NULL DEFAULT 'SYSTEM'`）、
   `app/schemas/…/12.json`、`UserSettingsBackup.themeMode`（带默认值兼容旧备份）、
   `YanjiMigrationTest` 新增 2 个用例（现共 11 个）。
5. **主题化 sweep：715 处 / 42 文件**，`ui/` 层静态亮色 token 归零（原 719 处）。
   亮色下逐值相等 ⇒ 视觉零变化（真机截图已核对）。
   连带修 31 处非 Composable 上下文（Canvas 颜色上提、顶层 val 改 @Composable getter、
   纯函数/组合子加注解、枚举 dotColor 改 @Composable 函数、remember lambda 内取色外提、
   局部非 Composable 函数改文件级组合子）。
6. **设置页**：`ProfileComponents.ProfileThemeSelector`（三档 chip，用 `selectable` 带 Selected 语义）
   + `ProfileScreen` 的「偏好」分组 + `ProfileTags` 测试锚点 + `MainActivity` 装配。
7. **暗色专属表现**：§3.2 底栏改半透明玻璃面板 + 微发光胶囊；§3.3 专注环底轨改 6% 白 +
   夜间自发光 + 运行态主蓝→微光紫渐变；§3.4 图表序列色暗色升调。
8. **门禁规则 3**：`scripts/check-design-tokens.sh` 新增「`ui/` 层禁止静态亮色 token」（0 容忍）。

## 验证（真机 PKB110 / Android 16）

- Unit `:app:testDebugUnitTest`：141 tests / 0 failures（含迁移 11 个，其中新增 2 个）
- Lint `:app:lintDebug`：**0 errors / 9 warnings**（与上一轮持平，无新增）
- Guard：颜色 0 / 圆角 73（≤74）/ 静态 token 0 ✓
- `assembleDebug` + `assembleRelease`：✅
- **真机插桩 `:app:connectedDebugAndroidTest`：42 tests / 0 failures**
  （新增 `ProfileThemeSelectorInstrumentedTest` ×4）
- 真机逐屏截图核对：暗色（首页/设置/偏好）与亮色（首页）均正确，亮色与改动前一致。

## 坑

- `theme_sweep.py` 第一版把**单行 import** 也替换了（`import …theme.YanjiTextPrimary` →
  `import …theme.MaterialTheme.colorScheme.onSurface`），产生 43 处废码。
  修法：跳过 `import ` 行 + 清理因 token 被换掉而失效的单行导入。
- `MAP` 曾漏掉 `YanjiBorder` / `YanjiDivider`（共 33 处未替换）→ 补上后归零。
- 并发进程的提交会把 `YanjiTheme` 默认值改回 `false`，重做时要复核锚点。
