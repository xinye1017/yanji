# 2026-09-12

## 记日记页重构（Stitch 设计稿 → JournalEditorScreen）

- 设计稿：Stitch 项目 `Kaoyan Diary App` screen `b738c33a...`（「研迹 - 记录今日日记」），HTML 存 `build/stitch_journal_design.html`。
- **数据层 v9→v10**：`journal_entries` 新增 `blockers TEXT NOT NULL DEFAULT ''`（遇到的困难/卡点）。
  - 迁移用 `ALTER TABLE ... ADD COLUMN ... DEFAULT ''`，与 MIGRATION_2_3 先例一致；
    **实测 Room 的 TableInfo 校验允许「entity 侧无 DEFAULT、DB 侧有 DEFAULT」**（全链测试 v1→10 用 10.json 校验通过）。
  - `app/schemas/.../10.json` 由 KSP 导出，需随代码提交。
  - Domain `JournalEntry.blockers` 默认 `""`，备份 JSON 向后兼容（BackupCodec 已有 ignoreUnknownKeys）。
- **JournalEditorScreen 全量重写**（ui/journal/JournalEditorScreen.kt）：
  - 顶部导航保持 FocusScreen 样式：返回按钮 + 24sp 大标题「今日日记/当日日记」+ 右侧「今日累计 Xh Xm」胶囊徽章（可点跳当日明细，新增 `onNavigateToDailyDetail` 回调，Navigation 已接线）+ 副标题「M 月 d 日 · 初试倒计时 N 天」（倒计时来自 `repo.settings.targetExamDate`，JournalViewModel 新增 `settings` 透传）。
  - 内容区按设计稿：今日专注摘要卡（subjectDistribution 科目分布，缩写 数/408/英/政）→ 五档心境 pill（映射 moodScore 1..5：需调整/微浮躁/平稳前行/专注充实/深度心流）→ 复盘输入(content) → 卡点输入(blockers) → 明日规划动态任务列表（tomorrowPlan 按换行拆分存储）→「保存今日足迹」大按钮。
  - 无底部导航栏（子页面）；title 输入移除（编辑时继承旧值，列表页有 fallback）；energyScore/studySatisfaction 不再展示、保存时继承。
  - 顺手修复：编辑已有日记时 `createdAt` 被刷新为当前时间的既有 bug（现继承原值）。
- 踩坑：YanjiMigrationTest 的 v9 起点 DDL 不能直接用 v7Ddl（journal 带 studyDurationSeconds、user_settings 带 aiApiKey），必须替换为 v8 重建后的表结构，否则终点校验失败。
- 验证：`:app:testDebugUnitTest`（76 用例）+ `:app:assembleDebug` 全绿。

## 真机验证（2026-09-12 05:40）

- 无线 ADB：mDNS 自动发现 `adb-3B1F6KE5SAR82D37-...`（OPPO PKB110，1256x2760）。
- **双重坑确认**：① Bash 会话 `TEMP=/tmp` 会让 adb.exe 的 server 起来即退，必须
  `export TEMP='C:\Users\dex\AppData\Local\Temp'`；② daemon 随 Bash 调用结束被杀，
  所以「等设备→操作→截屏」全部链在同一条命令里，且 mDNS 设备发现要等 3~30 秒（带轮询）。
- 流程：`build/adb-backup.sh` 备份（db+wal/shm → `build/device-backup/`）→ install -r -d → 启动。
- 设备库已是 v10（journal 0 行、blockers 列在位）；截屏确认编辑页与设计稿一致、无崩溃。
- 截图：`build/screen_editor.png`（上半屏）、`build/screen_editor_bottom.png`（含保存按钮）。

## 统计页优化（Stitch「研迹 - 学习统计」设计稿，22:40）

- 设计稿：screen `54ce48c210d943df9f3af99d9e5ec219`，HTML 存 `build/stitch_stats_design.html`；
  科目卡完整提取在 `build/stats_card_pretty.html`。
- **核心修复**：`buildSubjectDistribution` 数据源会返回**全量科目目录**（含 0 时长空科目），
  统计页列表会渲染空进度条。→ 过滤放在 **StatsViewModel**（flow `.map { filter{duration>0} }` + initial 两处），
  遵循「页面只渲染、派生计算在 VM」的约定；该函数唯一消费者就是 StatsViewModel，改动安全。
- UI（参考设计稿）：
  - 主卡片圆角 20 → 24dp（hero/最长专注/趋势/科目/AI 卡；三小指标卡保持 20dp，同设计稿）。
  - 科目卡：新增 `SubjectLevelSegmented`（设计稿式分段控件：浅灰容器+白 pill，大类在前）
    与 `SubjectDistributionBar`（分段比例条，8dp 高、段间 2dp 缝、两端胶囊圆角）。
  - 空态：科目列表为空时不再渲染任何行，改为一行居中提示「本周期暂无科目学时记录…」。
  - donut 保持原样（已有「暂无数据」空态环）。
- 验证：单测 + assembleDebug 全绿。真机推送待设备上线（22:44 时无线调试不在线，
  mDNS 60s 未发现）。

### 真机验证（22:48）

- **mDNS 发现实测要 ~40 秒**（不是 AGENT.md 写的 2 秒；本机跑着 tailscaled 可能拖慢组播）。
  → 推送链开头的设备轮询必须留足预算（30×5s），短等待会假性失败。
- 推送成功（黄金单行链式命令 + `export TEMP` 前缀，纯 UI 改动未跑备份，遵循新版 AGENT.md）。
- 截图确认：科目卡空态 = 灰环「暂无数据」+ 一行提示「本周期暂无科目学时记录，完成一次专注后自动生成」，
  **无空科目行/空进度条**；分段控件【大类/子类】与 24dp 圆角生效。
  设备本周无学习数据，有数据状态（比例条+列表）待真实计时后可见。
- 截图：`build/screen_stats_top.png`、`build/screen_stats_subject.png`。

### 统计页第二轮迭代（用户三点反馈，22:50-22:55）

1. **时间切换**：`PrimaryTabRow` → 设计稿式胶囊分段控件（浅灰 `RoundedCornerShape(50)` 容器 +
   白色选中胶囊 `Surface(shadowElevation=2dp)`）。
2. **hero 卡放大**：大数字 36→46sp（h/m 单位分离、textPrimary 深色）、行1 加「有效学习 N 天」+
   「较上周 ±X.Xh」pill（仅周视角；上周 = `getStudyDurationForPeriod(14) - (7)`，
   StatsUiState 新增 `previousWeekSeconds`）、行2 右侧「日均投入」、
   行3 目标进度条（周=dailyGoal×7 / 月=dailyGoal×30，settings 经 `viewModel.settings` 透传）。
3. **布局重排**：三小卡从 hero 下方挪到**科目卡之后、AI 卡之前**（对齐设计稿），
   内容改为「连续研读 / 单次最长 / 全真模拟」——`日均专注` 信息并入 hero 的日均投入，
   单次最长大卡删除（值+点击进详情折叠进小卡）。`LightweightMetricCard` → `MetricMiniCard`
   （圆形上色图标 28dp + 标签 + 18sp 加粗值 + 单位小字）。
- 真机验证：截图 `build/screen_stats2_top.png`（胶囊控件+hero）、`screen_stats2_bottom2.png`
  （三小卡新位置）。设备无学习数据，`较上周` pill 逻辑就绪但需真实数据才可见。
- 坑：`adb shell input swipe` 短时多次快速滑动（150ms）才滚得动，350ms 慢滑几乎不动。

