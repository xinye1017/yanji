

~~~md
# 任务：完整重构并完善「研迹」Android App 的学习记录交互与前端体验

你现在负责继续开发现有 Android 项目「研迹」。

不要只给方案、不要只输出代码片段、不要停留在 TODO。

请：

1. 先完整检查现有项目结构、Navigation、Room、Repository、ViewModel、Compose 页面和数据库实体；
2. 在现有代码基础上直接修改；
3. 保持现有视觉风格；
4. 完成下面所有功能和 UI 优化；
5. 编译项目；
6. 修复编译错误；
7. 尽可能运行测试；
8. 如果当前 ADB 真机可用，则安装 Debug 版本到已连接 Android 实体设备；
9. 最终给出修改摘要、关键文件、数据库变更和验证结果。

不要未经必要理由重写整个项目。

---

# 一、产品背景

产品名称：

**研迹**

定位：

一个仅供个人使用的 Android 考研学习记录 App。

主要功能：

- 考研倒计时
- 专注计时
- 科目学习时长
- 模拟考试
- 考研日记
- 学习统计
- AI 阶段复盘
- Local First 本地数据库

产品 IP：

**卷卷**

视觉风格：

- 蓝白
- 大圆角
- 简洁
- 留白
- 安静
- 克制
- 柔和
- Material 3 基础
- Floating Glass-inspired Navigation

minSdk：

**24**

不要依赖 API 31+ 的真正 backdrop blur。

---

# 二、本次重构的核心目标

目前 UI 已经基本成型。

本阶段不要继续堆更多表面统计数据。

核心目标变为：

> 所有重要统计数字都应该可以进一步追溯到真实的 FocusSession / ExamSession 数据。

建立完整的信息钻取链路：

统计
↓
每日 / 科目详情
↓
具体 Session
↓
Session 详情

最终让「研迹」真正成为一个：

**可查询、可回顾、可统计、可复盘的个人考研数据库。**

---

# 三、P0：必须优先修复的问题

## 3.1 修复 Floating Bottom Navigation 遮挡页面内容

当前 Floating Navigation 会遮住：

- 统计页底部内容
- 日记列表
- 首页部分卡片
- 其他一级页面最后的内容

所有使用 Floating Navigation 的一级页面必须统一增加足够的 bottom content padding。

推荐：

```text
bottomPadding =
navigationHeight
+ navigationBottomOffset
+ extraBreathingSpace
~~~

实际视觉建议：

```text
120dp ~ 132dp
```

必须保证：

页面最后一项内容可以完整滚动到 Floating Navigation 上方。

不能通过给某个页面单独硬编码临时修复。

建议统一抽象：

```text
Scaffold / ScreenContainer / AppContentInsets
```

所有一级页面复用。

------

# 四、Floating Navigation 保持现有玻璃风格

由于：

```text
minSdk = 24
```

不要使用真正的 backdrop blur 作为核心实现。

使用：

**Glass-inspired Floating Navigation**

通过以下方式模拟液态玻璃感：

- 半透明冷白背景
- 大圆角
- 细高光边框
- 柔和阴影
- 页面与导航栏之间的层次

推荐参数方向：

```text
background:
White alpha ≈ 0.80 ~ 0.86

border:
1dp
White alpha ≈ 0.65 ~ 0.75

cornerRadius:
28dp ~ 32dp

elevation:
约 8dp

horizontalMargin:
20dp

bottomOffset:
12dp ~ 16dp
```

不要：

- 强 blur
- 蓝紫渐变玻璃
- neon
- 发光
- 重阴影
- dark glass

导航栏需要视觉一致地运行在 API 24+。

------

# 五、统一底部导航图标

当前导航图标不统一。

统一改成：

**Phosphor Icons**

导航对应：

```text
首页
House

专注
Timer

日记
Notebook

统计
ChartBar

我的
UserCircle
```

未选中：

```text
Regular
#667085
```

选中：

```text
Fill
#356AE6
```

不要再混合：

- Material Icons
- Lucide
- 其他 Icon Pack

日记不要使用 Book。

使用：

```text
Notebook
```

统计不要使用金融意味明显的折线箭头。

使用：

```text
ChartBar
```

------

# 六、建立统一的数据 Drill-down 规则

请遵循以下产品原则：

> 只要用户可能会问“这个数字是怎么来的？”，这个数字就应该可以点击进入数据明细。

需要支持以下跳转：

```text
今日学习 7h32m
→ DailyStudyDetail

数学 3h42m
→ SubjectStudyDetail

本周学习 48h32m
→ WeeklyStudyDetail 或对应日期统计

日均学习 6h56m
→ 最近 7 天 / 本月每日明细

最长单次专注
→ FocusSessionDetail

模拟考试 4 次
→ ExamHistory

统计柱状图某一天
→ DailyStudyDetail

日记中的 7h32m
→ DailyStudyDetail
```

不要让统计数据只是静态展示。

------

# 七、新增 DailyStudyDetail 页面

新增页面：

```text
DailyStudyDetailScreen
```

Route 应接受：

```text
date
```

例如：

```text
/daily-study/2026-09-06
```

页面展示：

```text
← 2026 年 9 月 6 日

今日学习
7h 32m

共 6 次专注
```

下面使用时间轴或者非常清晰的 Session 列表。

推荐布局：

```text
08:30 — 10:18

数学
二重积分强化

1h 48m
```

下一条：

```text
10:35 — 11:22

英语
阅读理解

47m
```

模拟考试：

```text
13:30 — 16:30

数学模拟考试

3h 00m
126 分
```

需要区分：

```text
FocusSession
ExamSession
```

但视觉语言保持统一。

页面顶部展示：

```text
总学习时长
Session 数量
科目分布
```

不要变成 Dashboard。

------

# 八、新增 SubjectStudyDetail 页面

新增：

```text
SubjectStudyDetailScreen
```

Route：

```text
/subject-study/{subjectId}
```

支持时间范围切换：

```text
今日
本周
本月
全部
```

页面结构：

```text
← 数学

今日专注
3h 42m

共 3 次
最长一次 1h48m
```

Session 明细：

```text
08:30 — 10:18
二重积分强化
1h 48m

14:05 — 15:27
线性代数 · 特征值
1h 22m

19:40 — 20:12
错题复盘
32m
```

如果包含考试：

```text
13:30 — 16:30

数学模拟考试

3h
126 分
```

每一条 Session 都可以点击。

------

# 九、新增 FocusSessionDetail 页面

新增：

```text
FocusSessionDetailScreen
```

页面展示：

```text
数学

二重积分刷题

2026 年 9 月 6 日
08:30 — 10:18

持续时间
1h 48m

暂停
2 次 · 6m 12s

模式
正向计时

备注
完成二重积分第二型换元专题
```

底部：

```text
编辑记录
删除记录
```

删除必须二次确认。

如果现有数据库不足以记录：

```text
pauseCount
pausedDuration
mode
```

优先基于已有字段实现。

如果需要扩展 Room schema：

必须增加 Migration。

不要使用：

```text
fallbackToDestructiveMigration()
```

------

# 十、新增 ExamHistory / ExamDetail

统计页面：

```text
模拟考试 4 次
```

点击进入：

```text
ExamHistoryScreen
```

展示：

```text
数学模拟
2026-09-03
3h
126 分

408 模拟
2026-08-30
3h
118 分
```

支持查看详情。

ExamDetail 可以展示：

```text
科目
日期
开始时间
结束时间
实际用时
计划用时
分数
备注
状态
```

------

# 十一、首页重构

当前首页结构基本保留。

不要大规模重新设计。

重点增强交互。

------

## 11.1 今日学习可点击

例如：

```text
今日专注学习

7h 32m
```

点击：

```text
DailyStudyDetail(today)
```

整个卡片或主要数据区域都可以点击。

需要有轻微：

```text
press feedback
```

但不要明显 Button 化。

------

## 11.2 科目 Chip 可点击

现在：

```text
● 数学 3h42m
● 408 2h30m
● 英语 1h20m
```

全部改为可点击。

点击：

```text
SubjectStudyDetail(subjectId, today)
```

视觉仍然保持轻量 Chip。

可以增加非常轻微的：

```text
chevron
press state
```

但不要破坏简洁感。

------

## 11.3 首页总时长必须和科目数据一致

当前 Mock UI 可能出现：

```text
今日学习
0h 0m
```

但是下面却有：

```text
数学 3h42m
408 2h30m
英语 1h20m
```

正式实现禁止出现这种情况。

建立单一数据源：

```text
StudyStatisticsRepository
```

例如：

```text
todayTotalDuration =
SUM(FocusSession today)
+
SUM(ExamSession today)
```

科目分布和总时长必须来自同一套 Query / Repository。

不要让 Compose UI 自己计算不同版本的数据。

------

# 十二、首页快捷操作优化

目前：

```text
开始专注
模拟考试
写今日日记
```

三个操作视觉权重过于接近。

建议调整为：

```text
Primary:
开始专注
```

更突出。

Secondary：

```text
模拟考试
写今日日记
```

可以稍微弱化。

但不要完全推翻现有布局。

如果当前三卡设计已经稳定，可通过：

- Primary 更深蓝
- Secondary 更浅
- 尺寸层级

实现。

不要加入 FAB。

------

# 十三、专注页优化

## 13.1 修复 Chip 横向被截断

当前：

```text
数学
408专业课
英语
政治
...
```

以及：

```text
正向计时
45分钟番茄
60分钟深度
90分钟...
```

右侧会显示半个 Chip。

这种效果不要保留。

使用：

```text
FlowRow
```

自动换行。

例如：

```text
数学
408专业课
英语

政治
其他
```

计时模式同理：

```text
正向计时
45分钟番茄

60分钟深度
90分钟深度
```

不要依赖横向滚动提示。

------

## 13.2 专注页增加今日记录摘要

如果今天没有记录：

```text
今天还没有留下学习轨迹
```

保持当前 Empty State。

如果已有记录：

显示：

```text
今日已专注

6h 20m · 4 次
```

下面最多展示最近 2~3 条：

```text
数学
08:30 — 10:18
1h48m

英语
10:35 — 11:22
47m
```

底部：

```text
查看今日全部记录 →
```

点击进入：

```text
DailyStudyDetail(today)
```

这样形成：

```text
开始专注
→ 完成
→ 产生记录
→ 当天历史
```

完整闭环。

------

# 十四、日记页面优化

## 14.1 日记上的学习时长可点击

例如：

```text
2026-09-05
7h32m
★★★★★
```

其中：

```text
7h32m
```

必须可以点击。

跳转：

```text
DailyStudyDetail(2026-09-05)
```

让：

```text
主观日记
```

和：

```text
客观学习行为
```

真正关联。

------

## 14.2 日记编辑从 Dialog 改成全屏页面

当前日记编辑使用大型 Dialog。

请重构为：

```text
JournalEditorScreen
```

不要使用大型 Modal Dialog。

原因：

- 长文本输入
- Android 软键盘
- 多字段编辑
- 滚动体验

都更适合独立页面。

推荐：

```text
← 9 月 6 日日记                保存

今日状态
★★★★★

今日标题

[........................]

今日状态与总结

[........................
........................
........................]

明日核心目标

[........................]
```

底部可显示：

```text
今日学习
6h 00m
```

只读。

保存成功：

```text
popBackStack()
```

不要多弹一个成功 Dialog。

------

# 十五、统计页重构

当前统计页有：

```text
本周学习
日均专注
最长单次专注
模拟考试次数
```

四个等权 2×2 卡片。

稍微过于 Dashboard 化。

调整视觉层级。

------

## 15.1 主指标

突出：

```text
本周学习
48h 32m
```

可以增加：

```text
比上周 +3h16m
```

如果已有相应数据。

------

## 15.2 Supporting Metrics

弱化为一行或轻量卡片：

```text
日均
6h56m

连续学习
7天

模拟考试
4次
```

------

## 15.3 最长单次

放到：

```text
学习特征
```

或者后续信息区域。

仍可点击进入对应 FocusSessionDetail。

------

# 十六、统计图增加交互

当前 7 天柱状图：

```text
周日
周一
周二
...
```

每一根柱子都应可点击。

点击后：

优先展示 BottomSheet：

```text
9 月 5 日 · 周五

8h 21m

数学
3h45m

408
2h56m

英语
1h40m

查看当天记录 →
```

点击：

```text
查看当天记录
```

进入：

```text
DailyStudyDetail
```

BottomSheet：

- 28dp 上圆角
- 白底
- 简洁
- 不要变成弹窗 Dashboard

------

# 十七、科目统计可点击

统计页科目分布：

```text
数学 18h22m
408 ...
英语 ...
```

每一项都可以点击：

```text
SubjectStudyDetail(subjectId)
```

这样建立：

```text
Statistics
→ Subject
→ Session
```

------

# 十八、连续学习指标

如果已有：

```text
连续有效学习 7 天
```

可以点击打开一个轻量日历 / Streak 页面。

如果本阶段实现成本过高：

暂时允许只实现：

```text
点击 → 最近有效学习日列表
```

但不要因此影响核心功能进度。

此项优先级低于：

```text
DailyStudyDetail
SubjectStudyDetail
FocusSessionDetail
```

------

# 十九、数据库 / Repository 设计

保持 Local First。

建议建立或完善：

```text
StudyStatisticsRepository
```

提供统一接口：

```text
getTotalDuration(date)
getSubjectDurations(date)
getDailySessions(date)
getSubjectSessions(subjectId, range)
getWeeklyStatistics()
getMonthlyStatistics()
getLongestSession(range)
getExamCount(range)
getStudyStreak()
```

尽量把 SQL 聚合放在：

```text
DAO
```

或者 Repository。

不要在 Compose 页面里遍历数据库结果手工统计大量数据。

------

# 二十、学习时长的唯一真实来源

不要保存并维护：

```text
totalStudyTime
```

这种累计变量。

真实数据来源：

```text
FocusSession
ExamSession
```

统计时动态聚合。

规则：

```text
TotalStudyDuration =
Valid FocusSession duration
+
Valid ExamSession duration
```

确保不要重复计算。

例如：

如果 ExamSession 同时生成了 FocusSession：

必须只选择其中一种作为统计来源。

检查当前实现。

不能出现重复累计。

------

# 二十一、页面 Navigation

至少增加 Routes：

```text
DailyStudyDetail
SubjectStudyDetail
FocusSessionDetail
ExamHistory
ExamDetail
JournalEditor
```

推荐类型安全参数。

避免到处手写不稳定 String Route。

如果项目已经使用 Navigation Compose typed routes：

继续沿用现有方式。

------

# 二十二、视觉风格保持不变

当前视觉方向已经确定。

继续遵循：

```text
Primary:
#356AE6

Background:
#F7F9FC

Surface:
#FFFFFF

Primary Soft:
#EAF1FF

Text Primary:
#172033

Text Secondary:
#667085

Lavender:
#8B7CF6
```

圆角：

```text
Card
20~28dp
```

重点：

- 大留白
- 蓝白
- 少边框
- 柔阴影
- 少量薰衣草紫
- 不游戏化
- 不企业 Dashboard
- 不 neon
- 不 glassmorphism 泛滥

不要因为增加新页面而改变现有品牌设计。

------

# 二十三、交互反馈

所有可点击统计项需要：

```text
pressed state
```

但是：

不要使用很强的 Ripple。

保持 Android 原生可感知即可。

卡片可以：

```text
轻微背景变化
或者
轻微 scale / elevation change
```

------

# 二十四、空状态

如果 DailyStudyDetail 没有记录：

显示：

卷卷（如果现有资源已经接入）

```text
今天还没有留下学习轨迹。
```

按钮：

```text
开始专注
```

如果 Subject 没有记录：

```text
这个科目还没有专注记录。
```

不要显示假数据。

------

# 二十五、Loading / Error

Room 本地查询：

使用：

```text
Flow / StateFlow
```

正常情况下不需要复杂 Loading。

AI 页面保持原有逻辑。

本次重构不要影响 AI API。

------

# 二十六、必须保证的数据一致性

重点验证：

### 今日总时间

必须等于：

```text
所有当天有效 Session 之和
```

### 科目总时间

必须等于：

```text
对应 Subject 的 Session 之和
```

### Weekly Total

必须等于：

```text
7 天 daily total sum
```

### Journal 学习时长

必须读取：

```text
对应 date 的 StudyStatistics
```

不要存一份独立副本。

------

# 二十七、实现优先级

严格按以下顺序执行。

## P0

必须全部完成：

- Floating Navigation 不再遮挡页面
- FlowRow 修复 Chip 截断
- 首页统计统一数据来源
- 数据时长计算一致

## P1

必须全部完成：

- DailyStudyDetail
- SubjectStudyDetail
- 首页科目 Chip 点击
- 今日学习点击
- 统计柱状图点击
- 日记学习时长点击
- 专注页今日历史

## P2

尽量全部完成：

- FocusSessionDetail
- ExamHistory
- ExamDetail
- JournalEditor 全屏化
- Stats 页面降低 Dashboard 感

## P3

有余力完成：

- Streak Detail
- 微动效
- 卷卷 Empty State
- 更细致的 Bottom Sheet

------

# 二十八、代码质量要求

不要：

- 为了赶功能把业务逻辑全部塞进 Composable
- 在 UI 里直接访问 DAO
- 到处重复 Date / Duration 格式化逻辑
- 到处重复统计代码
- 写魔法数字
- 使用 fallbackToDestructiveMigration
- 删除现有数据库
- 破坏用户已有本地数据

需要适当抽取：

```text
DurationFormatter
DateFormatter
StudyStatisticsRepository
Reusable SessionRow
SubjectDurationChip
FloatingBottomNavigation
```

但不要为了架构美观过度 Clean Architecture。

项目只有一个用户和一个开发者。

保持：

```text
Compose
↓
ViewModel
↓
Repository
↓
Room
```

即可。

------

# 二十九、数据库 Migration

如果增加 Room 字段或表：

必须：

1. bump database version；
2. 创建 Migration；
3. 保留已有数据；
4. 验证旧数据库可以升级。

严禁：

```text
clear app data
```

来掩盖 Migration 问题。

------

# 三十、真机验证

当前 Android 手机已经连接 ADB Wireless Debugging。

完成开发后执行：

```bash
adb devices
```

确认设备。

然后至少执行：

```bash
./gradlew assembleDebug
```

如果项目支持：

```bash
./gradlew test
```

再执行。

安装：

```bash
adb install -r <debug-apk>
```

或者：

```bash
./gradlew installDebug
```

注意：

```text
-r
```

保留现有 App 数据。

不要卸载 App 后重新安装，除非确实发生 package/signature 冲突。

------

# 三十一、真机至少验证以下流程

## Flow A

```text
首页
→ 点击数学 3h42m
→ 数学详情
→ 点击某条 Session
→ Session Detail
```

------

## Flow B

```text
首页
→ 今日学习
→ DailyStudyDetail
```

------

## Flow C

```text
统计
→ 点击周五柱子
→ BottomSheet
→ 查看当天记录
→ DailyStudyDetail
```

------

## Flow D

```text
日记
→ 点击某天 7h32m
→ 当天学习详情
```

------

## Flow E

```text
专注
→ 选择科目
→ 开始专注
→ 结束
→ 今日记录立即刷新
```

------

## Flow F

```text
日记
→ 写今天
→ JournalEditor 全屏页面
→ 保存
→ 返回日记列表
```

------

## Flow G

```text
杀掉 App
→ 重新启动
→ 数据仍然存在
→ 统计数字一致
```

------

# 三十二、验收标准

整个任务完成后必须满足：

-  Navigation 不遮挡内容
-  Subject / Mode Chip 不会只显示半个
-  首页今日总时长与科目总和一致
-  首页科目可以点击
-  今日总时长可以点击
-  DailyStudyDetail 可用
-  SubjectStudyDetail 可用
-  Session 可以查看详情
-  模拟考试记录可以查看
-  统计图可以点击查看当天信息
-  Journal 学习时间可以进入当天记录
-  Journal Editor 不再使用大型 Dialog
-  专注页可以看到今天已经完成的 Session
-  所有统计来自统一 Repository
-  没有重复累计 ExamSession
-  Room Migration 安全
-  原数据不会因为升级丢失
-  Debug Build 成功
-  真机可以启动
-  主要页面没有明显遮挡 / Overflow
-  UI 风格仍然保持现有研迹设计语言

------

# 三十三、最终汇报格式

全部完成后，只需向我汇报：

## 完成内容

列出已实现功能。

## 关键文件

列出主要修改 / 新增文件。

## 数据库变化

说明：

- version
- migration
- schema changes

## 页面变化

列出：

- 新页面
- 新跳转
- 新交互

## 测试

说明：

- Gradle Build
- Tests
- ADB install
- 真机验证结果

## 未完成项

如果确实存在无法完成的问题，必须明确写出：

```text
原因
影响
下一步
```

不要用 TODO 代替实现。

------

# 最终产品原则

请始终遵循：

> 不要继续增加更多孤立的统计数字。

而是：

> 让现有的每一个数字，都能追溯到真实的学习记录。

最终用户应该形成这样的使用体验：

```text
我看到今天学习了 7h32m
↓
我可以看到这 7h32m 是怎么组成的
↓
我可以看到数学用了多久
↓
我可以看到数学分别在哪几个时间段学习
↓
我可以打开某一次专注
↓
我可以回顾当时学了什么
↓
我可以结合当天日记理解自己的状态
```

这就是本次重构最重要的目标。

现在开始检查现有代码，并直接执行整个任务。

```
我认为这版已经比较适合“一键扔给 Agent 跑到底”了。它不只是告诉 Agent **做什么页面**，还规定了**数据应该怎么流动、哪些页面互相跳转、哪些属于 P0/P1、数据库升级不能怎么做，以及最后怎么真机验收**。

尤其最重要的一条被固定下来了：

> **`FocusSession / ExamSession → Repository 聚合 → 统计 → Detail → Session`**

后面哪怕继续加入 AI 复盘、周报或者月报，也可以直接建立在这套数据链路上，而不需要重新推翻架构。
```