# 专注计时系统重构报告

> Focus Environment + Android 16 Live Update + ColorOS 流体云
> 实测机型：OPPO PKB110 · ColorOS **V16.1.0** · Android 16 · `ro.build.version.sdk_full = 36.0`
> 完成时间：2026-09-13

---

## 1. 修改了哪些文件

### 新增（源码）

| 文件 | 行数 | 作用 |
| :--- | ---: | :--- |
| `data/timer/FocusLiveState.kt` | 182 | 统一状态模型 + 派生映射 + 全局唯一时间格式器 |
| `liveactivity/FocusNotificationSpec.kt` | 190 | 平台无关的通知描述 + 状态→描述的纯函数工厂 |
| `liveactivity/FocusTimerActions.kt` | 23 | 通知 Action 字符串的唯一定义 |
| `liveactivity/StandardNotificationController.kt` | 128 | spec → `NotificationCompat.Builder`（Chronometer、动作） |
| `liveactivity/AndroidLiveUpdateController.kt` | 86 | Promoted Ongoing + `ProgressStyle` + 下发前自检 |
| `liveactivity/FocusLiveActivityController.kt` | 119 | 常驻展示层唯一入口（三级降级决策） |
| `liveactivity/LiveActivityCapability.kt` | 116 | 运行时能力探测（异常一律降级） |
| `liveactivity/coloros/ColorOsFluidCloudController.kt` | 103 | ColorOS 能力描述 + 用户引导，**无私有接口调用** |
| `res/drawable/ic_stat_focus.xml` | 12 | 单色秒表通知图标 |

### 新增（测试）

| 文件 | 用例数 | 作用 |
| :--- | ---: | :--- |
| `test/.../data/timer/FocusLiveStateTest.kt` | 7 | 状态迁移矩阵 + 时间格式边界（JVM） |
| `test/.../liveactivity/FocusNotificationSpecsTest.kt` | 9 | Chronometer 基准算法、暂停冻结、动作切换（JVM） |
| `androidTest/.../liveactivity/LiveActivityCapabilityInstrumentedTest.kt` | 2 | 真机能力报告落盘 + 通知可提升性不变量的守门测试 |

### 修改

| 文件 | 改动 |
| :--- | :--- |
| `service/FocusTimerService.kt` | **删除计时循环里的每秒 `updateNotification()`**；新增 `liveState` / `elapsedSecondsForUi` 两条流；通知全部委托控制器；动作常量改为引用 `FocusTimerActions` |
| `ui/focus/FocusScreen.kt` | 改消费 `liveState`（语义）+ 转发 `State<Long>`（每秒），**不在函数体内取值** |
| `ui/focus/ActiveFocusContent.kt` | 签名改为 `elapsedSeconds: State<Long>`，秒数读取下沉到两个叶子；时间格式改用共享 `formatFocusClock` |
| `androidTest/.../ActiveFocusContentInstrumentedTest.kt` | 跟随签名改动传 `State<Long>` |
| `AndroidManifest.xml` | 增加 `POST_PROMOTED_NOTIFICATIONS`（字面量，见 §17） |

> 注：`Navigation.kt`（专注时隐藏底栏）、`QuietFocusSetupContent.kt`、`AchievementSummaryBanner.kt`、`GlassBottomBar.kt`、`ProfileScreen/ViewModel` 是本次之前已在工作区、尚未提交的改动，不在本次重构范围内。

---

## 2. 每个文件做了什么

见上表「作用」列。分层原则：**状态（data/timer）→ 文案契约（liveactivity 纯 Kotlin）→ 平台调用（Controller）→ 生命周期（Service）**，四层单向依赖，Controller 不含业务判断，Service 不含平台细节。

---

## 3. TimerMachine 是否保持原样

**是，一行未改。**

它已经是正确的实现：`accumulatedActiveMs` + `resumedAtMonotonicMs` + `MonotonicClock`。规范要求「如果当前计时逻辑已经正确，不要重写」——本次严格遵守。

---

## 4. 真实计时机制

- 时间事实 = **单调时钟差值**：`SystemClock.elapsedRealtime()` 差（`SystemMonotonicClock`）。
- 有效时长 = `accumulatedActiveMs + (now - resumedAtMonotonicMs)`。
- 每秒 tick **只用于**：推进 UI 展示镜像、检测倒计时归零。**不参与精度**。
- 挂钟时间只在一处使用：把剩余/已用时长投影成 `setWhen` 需要的绝对时间点，供 SystemUI 渲染 Chronometer。

---

## 5. Pause 时间如何排除

`TimerMachine.pause()` 把当前 running 段结算进 `accumulatedActiveMs`，并把 `resumedAtMonotonicMs` 置空；`resume()` 重新取单调时钟起点。因此暂停区间**从未进入** `elapsedMs()`。

展示侧的对应处理：暂停时通知**关闭 Chronometer** 改静态冻结文本（有真机插桩断言守门），避免系统继续走数字导致「看到的时长 = 有效时长 + 暂停时长」。

`FocusSession.pausedDurationSeconds` 由 `machine.pausedMs(nowEpochMs)` 单独计算落库。

---

## 6. App 内 Countdown 最终设计（Precision Instrument）

真机渲染见 `build/dev_focus-countdown.png`。

- **删除**：Ring 内部白色实体圆盘、Circle Surface、内层 Border、内层 Shadow、多层同心圆、钟表表盘感。
- **保留**：极淡 Track（主色 8% alpha）、蓝色进度弧、大数字、一行辅助状态（`已完成 N%`）。
- 环：`widthIn(max = 228.dp)` + `aspectRatio(1f)`；描边 **4dp**（原 10dp 粗环已去掉）。
- **起始圆点副作用**：用 `StrokeCap.Butt` + `progress > 0` 才绘制 → 启动瞬间不会出现 `●`。
- 进度用 `animateFloatAsState(tween(1000, LinearEasing))`，与秒推进同频，弧线连续无跳变。
- 数字：52sp、`FontWeight.Normal`、`letterSpacing = -1sp`、`fontFeatureSettings = "tnum"`，
  并按键位模板（数字位全替换成 `8`）预测量字号，保证 `59:59 → 1:00:00` 不改变字号、不水平跳动。

---

## 7. App 内 CountUp Flow Field 最终设计

真机渲染见 `build/dev_focus-flow.png`。

- **彻底删除**圆形白色 Surface、实体表盘、圆形 Border、Progress Ring、Shadow、多层同心圆。
- 时间**直接悬浮**：中央 `18:42`（60sp / Normal / tnum）。
- 页面中央是**没有边界的**径向呼吸场：`Canvas` + `Brush.radialGradient`，
  `0f → 主色`、`0.4f → 主色 35%`、`1f → 完全透明`，外缘自然消失，不存在「一个清晰蓝圆」。
- 构图（自定义 `Layout`，非 `SpaceBetween`）：顶部状态锚在可用高度 10%、主视觉中心 46%、Controls 贴底；
  空间不足时整页可滚动，大字体下三个锚点不重叠。

---

## 8. 呼吸动画最终参数

| 项 | 值 |
| :--- | :--- |
| Scale | `1 + phase × 0.015` → **0.985 ↔ 1.015** |
| Alpha | `(0.09 + phase × 0.03) × presence` → **0.06 ↔ 0.12** |
| 单程 | **3600 ms** |
| 完整周期 | **7200 ms**（6.4–8 s 区间内） |
| 缓动 | `FastOutSlowInEasing` |
| 暂停 | 相位 360 ms 平滑归 0（scale→1.0），同时 `presence` 360 ms 降到 **0.2** → 呼吸几乎收平，「空间静下来」 |
| 恢复 | 反向平滑恢复，不跳变 |

对比原实现（0.96↔1.04 / 2400 ms）幅度减半以上、周期拉长 3 倍。

---

## 9. 是否使用 graphicsLayer

**是。** `FlowBreathingField` 用 `Modifier.graphicsLayer { scaleX/scaleY/alpha }`，
动画值在**绘制层**读取，不经过 `Modifier.size()`，因此不触发布局与重测量（规范 §6/§37）。

---

## 10. Notification 是否还每秒 notify

**否。** `FocusTimerService` 的计时循环里已删除 `updateNotification()`。
通知只在语义转换时重建：**START / PAUSE / RESUME / FINISH / DISCARD**。

结构上也堵死了这条路：通知层只订阅 `liveState`（仅转换时发射），
每秒的 `elapsedSecondsForUi` 明确标注为「只给 Compose，通知层不准消费」。

---

## 11. Chronometer 如何实现

| 状态 | `setUsesChronometer` | `setChronometerCountDown` | `setWhen` | 正文 |
| :--- | :--- | :--- | :--- | :--- |
| 倒计时运行 | true | **true** | `now + remaining × 1000` | `剩余时间 · 保持专注` |
| 正向计时运行 | true | false | `now − elapsed × 1000` | `已专注 · 保持专注` |
| 倒计时暂停 | **false** | — | `showWhen = false` | `20:18 · 已暂停`（冻结） |
| 正向计时暂停 | **false** | — | `showWhen = false` | `32:18 · 已暂停`（冻结） |

已验证签名与 API 级守卫（反编译 `androidx.core:core:1.18.0`）：
`setUsesChronometer(boolean)`、`setChronometerCountDown(boolean)`、`setRequestPromotedOngoing(boolean)`、
`setShortCriticalText(String)`、`NotificationManagerCompat.canPostPromotedNotifications()`、
`NotificationCompat.hasPromotableCharacteristics(Notification)`、`NotificationCompat.ProgressStyle`。

**一处刻意的取舍**：规范 §13 的 `44:55 剩余` 与 §15/§19 的「必须用系统 Chronometer、禁止每秒 notify」
在字面上冲突——Chronometer 由 SystemUI 拥有数字。因此**数字交给 Chronometer，正文只承载语义后缀**，
不写成静态 `44:55`（那会显示一个不走的假时间）。暂停态则相反：数字是冻结的真实值，直接写进正文。

---

## 12. Android 16 Live Update 是否成功

**代码已完整实现；本机系统闸门关闭，实测降级为普通常驻通知。**

`AndroidLiveUpdateController` 会：`setRequestPromotedOngoing(true)` → 追加 `ProgressStyle`（倒计时）
或标准模板（正向计时）→ 下发前用 `hasPromotableCharacteristics()` 自检并记录结果。

---

## 13. `canPostPromotedNotifications()` 结果如何处理

真机实测值（`build/live-activity-capability.txt` + logcat）：

```
capability: sdk=36 promotedOngoing=false promotedPerm=true notifications=true
            colorOsFamily=true nativeFluidCloud=false tier=STANDARD_ONGOING brand=OPPO/OPPO
android.permission.POST_PROMOTED_NOTIFICATIONS: granted=true
NotificationChannel yanji_timer_channel: mImportance=2   ← IMPORTANCE_LOW，高于 MIN
AppSettings: com.example.yanji importance=DEFAULT userSet=true promoted=true
```

处理方式（`LiveActivityCapability.tier`，三级降级）：

| 条件 | 层级 |
| :--- | :--- |
| 通知总开关关闭 | `NOTIFICATIONS_DISABLED`（跳过下发，计时继续） |
| `canPost` && ColorOS 系 | `COLOR_OS_FLUID_CLOUD`（标准 Live Update，由系统侧呈现） |
| `canPost` | `ANDROID_LIVE_UPDATE` |
| **本机实际** | **`STANDARD_ONGOING`** ← 普通常驻通知 + Chronometer |

探测本身包了 `runCatching`：部分 Android 16 初版 ROM 可能缺 36.1 的 framework 方法
（`NoSuchMethodError`），任何异常一律降级，绝不影响计时。

> 注意：`dumpsys notification` 里 AppSettings 的 `promoted=true` **不等于** `canPostPromotedNotifications()`
> （`com.example.yanji.test` 同样是 `true`）。只信运行时 API。

---

## 14. ColorOS 16 是否出现前摄流体云

**未出现。** 原因是 §13 的系统闸门关闭——连标准状态栏胶囊都没有，流体云自然不会触发。

不是代码问题：`NotificationManagerCompat.canPostPromotedNotifications()` 返回 `false`，
应用侧没有任何合法手段绕过（规范也明确禁止 Reflection / Hidden API / Hook SystemUI）。

**待用户侧确认的两种可能**（我无法从应用侧区分）：
1. ColorOS 的「实时活动 / 流体云」需要用户在系统设置里对本应用**单独开启**；
2. 该 ROM 虽为 36.0 但未启用 36.1 的 promoted 能力。

打开系统开关后重跑 `LiveActivityCapabilityInstrumentedTest` 即可立刻看到新结论。

---

## 15. 标准 Android Live Update 是否自动映射到流体云

**未能在本机证实。** 公开资料（OPPO ColorOS 设计团队口径）称 ColorOS 16 已完整接入原生
Android 16 Live Updates API，遵循该规范的应用可直接适配；但这在本机**无法验证**，
因为前提条件（promoted 可用）不成立。

同时我在系统设置里找到了 ColorOS **自己的**门控，说明它不是纯转发：

```
fluid_cloud_not_support = 0                              ← 本机支持流体云
fluid_function_request = 1
fluid_settings_capsule_to_card = 1
livealert_disable_seedling = com.application.zomato;     ← 泛在服务按包名 denylist
oplus_no_fluid_capsule_animation = com.android.incallui  ← 小胶囊动画按包名 denylist
```

→ 结论：**不能假设 Android Live Update ≡ 流体云**，OEM 确实有额外的 eligibility 判定（规范 §24 已预警）。

---

## 16. 是否最终使用 SeedlingSupportSDK

**否，且不应使用。** OPPO 开放平台《流体云 接入准备》(`open.oppomobile.com/documentation/page/info?id=13572`)
明确要求接入方先提交 **QPS 峰值 / 单日请求总量 / 应用包名 / 开发者 ID**，并
**「联系 OPPO 进行确认」**；首次接入需**完成企业认证**；卡片需分配 `serviceId` 并开发 UPK。
其 SDK 坐标（`com.oplus.pantanal.card:seedling-support-*`）也需要商务开通后才有意义。

这是**商务接入**，不是技术选型问题，因此 `ColorOsFluidCloudController` 只做能力描述与降级，
不写任何私有调用。

---

## 17. 使用了哪些 OPPO 官方确认 API

**零个。** 本次没有调用任何 OPPO 私有/未公开接口，也没有引入任何 OPPO SDK。

ColorOS 相关代码只使用公开 Android API：`Build.MANUFACTURER/BRAND`（**仅作辅助信息**）、
`Settings.ACTION_APP_NOTIFICATION_SETTINGS`、`NotificationManager.getNotificationChannel()`。

一个必须记录的发现：`android.permission.POST_PROMOTED_NOTIFICATIONS` 与
`Settings.ACTION_MANAGE_APP_PROMOTED_NOTIFICATIONS` **都不存在于 API 36 的 `android.jar`**
（已把 6227 个 class 全量提取搜索确认）。所以：权限只能在 Manifest 里用**字面量**声明，
设置页常量**不可引用**（直接引用会编译失败）。

---

## 18. 是否实现 Ongoing_Indicator

**否。** 该能力只存在于 OPPO 原生泛在卡片体系（需 serviceId + UPK），见 §16。
下发给系统的只有标准 `ProgressStyle` 与 Chronometer，未做任何指示器动画注入。

---

## 19. 正向计时 collapsed capsule 是否允许动画

**无法从官方资料确认，标记为「待真机验证」。**

我找到的唯一硬证据是 ColorOS 的按包名开关 `oplus_no_fluid_capsule_animation`
（当前值只含 `com.android.incallui`）。它证明**胶囊动画确实是 ROM 侧按包名管控的**，
且本应用不在禁列——但这不等于「允许第三方用自绘动画」。

---

## 20. 如果不允许，降级方案是什么

**结构上已经不可能违规**：

- 绝不通过每几百毫秒 `notify()` 或每秒切换 icon 伪造呼吸（规范 §30 明确禁止）；
- 小胶囊文本 = `shortCriticalText`，只在**语义转换**时下发；运行中的数字由系统 Chronometer 拥有；
- 呼吸动画只存在于两处**确定允许**的地方：App 内 Flow Field（`graphicsLayer` 绘制层动画），
  以及（若将来接入）Expanded 流体云。

---

## 21. Expanded Fluid Cloud 是否实现 breathing

**否**——Expanded 流体云属 OPPO 原生卡片能力（需 serviceId + UPK），未接入，见 §16。

它在 App 内的等价物就是 Flow Field 的呼吸场（§8），两者共享同一套参数：
scale 0.985↔1.015、alpha 0.06↔0.12、单程 3600 ms。

---

## 22. 用户需要开启哪些 ColorOS 设置

按顺序自查（前两步决定 `canPostPromotedNotifications()` 能否翻成 `true`）：

1. **设置 → 通知与状态栏 → 应用通知管理 → 研迹** → 打开通知开关；
   找到本应用的「实时活动 / 流体云 / 状态栏胶囊」一类子开关并打开。
2. **设置 → 应用管理 → 研迹 → 通知** → 把 `研迹 · 专注与模考前台服务` 渠道设为**允许**，
   且不要设为「静默/不重要」（`IMPORTANCE_MIN` 会让提升必然失败）。
3. 不要在电池优化里限制研迹（前台服务被冻结会中断计时展示）。
4. 打开开发者选项的「无线调试」以便复测。

应用内已提供跳转入口：`ColorOsFluidCloudController.notificationSettingsIntent()`
使用公开的 `ACTION_APP_NOTIFICATION_SETTINGS`。

---

## 23. 普通 Android 如何降级

`tier = STANDARD_ONGOING`：一条 `IMPORTANCE_LOW` 的常驻通知，
内含系统 Chronometer（倒计时递减 / 正向递增）、`暂停|结束` 或 `继续|结束` 两个动作、
`ic_stat_focus` 单色小图标、无 LargeIcon。**不创建假 MediaSession、不用 MediaStyle**（规范 §13/§39）。

---

## 24. Android 15 及以下如何降级

`LiveActivityCapabilityDetector` 在 `SDK_INT < 36` 时**直接判定 `canPostPromotedOngoing = false`**，
不调用任何 36+ 方法 → 同一套标准常驻通知路径，无分支差异。

已通过反编译确认不需要额外兼容处理：`setRequestPromotedOngoing` 在 `SDK_INT < 36` 只是写一个
被忽略的 extra；`setShortCriticalText` 在 `SDK_INT < 36` 写 extra；`ProgressStyle.apply()` 在
`SDK_INT < 36` 直接跳过。三者都不会在低版本抛异常。`minSdk = 24` 的 `setChronometerCountDown` 要求满足。

---

## 25. 还有哪些 OEM / SystemUI 限制

1. **promoted 的最终决定权在系统**：应用只能「申请 + 自检」，系统可静默拒绝。本次即为实例。
2. **ColorOS 有独立的 livealert/seedling/pantanal 体系**与按包名 denylist，与标准 API 并行存在。
3. **渠道重要性一旦被用户改动，应用无法改回**（渠道属性归用户所有）。
4. **`dumpsys` 的字段语义不可全信**：`AppSettings.promoted=true` 与真实能力不一致。
5. **ColorOS 通知渠道存在 `mUnimportant=true`、`mFold=true` 等厂商扩展字段**，
   会影响展示形态，但不影响计时正确性。
6. **厂商扩展字段（`mFold` / `mUnimportant` / `oplus_*` 设置键）没有公开协议**，
   不要基于它们做业务判断。

---

## 26. 编译结果

| 任务 | 结果 |
| :--- | :--- |
| `:app:testDebugUnitTest` | **通过**（`FocusLiveStateTest` 7/7、`FocusNotificationSpecsTest` 9/9，全量单测无失败） |
| `:app:assembleDebug` | **通过** |
| `:app:assembleDebugAndroidTest` | **通过** |
| `:app:lintDebug` | **通过**（修复 2 个自引入错误：`MissingPermission`、`WrongConstant`；现 0 error） |
| 真机 `:app:connectedDebugAndroidTest` | **16/16 通过**（PKB110 - 16，0 failed / 0 skipped） |

修复的两个 lint 错误都是真实隐患，不是抑制掉了事：
`notify()` 增加 `areNotificationsEnabled()` 运行期守卫 + 显式 `catch (SecurityException)`；
`setVisibility` 改用 `NotificationCompat.VISIBILITY_PRIVATE`（规范 §38 要求「不要只让它能跑」）。

---

## 27. 真机测试步骤

### 已完成（自动化，可复现）

```bash
# JVM 单测
./gradlew.bat :app:testDebugUnitTest

# 真机能力诊断 + 通知契约（报告落盘到设备 externalCacheDir）
export ANDROID_SERIAL="adb-XXXX._adb-tls-connect._tcp"
./gradlew.bat :app:connectedDebugAndroidTest \
  -Pandroid.testInstrumentationRunnerArguments.class=com.example.yanji.liveactivity.LiveActivityCapabilityInstrumentedTest
adb pull /sdcard/Android/data/com.example.yanji/cache/live-activity-capability.txt .

# 全量插桩
./gradlew.bat :app:connectedDebugAndroidTest
```

### 需要人工在设备上完成（我未执行，避免占用你正在使用的手机）

1. 打开 §22 的系统开关，重跑上面第 2 条命令，对比 `promotedOngoing` 是否翻为 `true`。
2. 启动 45 分钟倒计时 → 下拉通知栏：确认倒计时**递减**、动作为 `暂停|结束`。
3. 返回桌面 / 锁屏 / 息屏 / AOD：确认通知仍在走；观察状态栏是否出现胶囊。
4. 点通知里的「暂停」→ 确认数字**冻结**、动作变 `继续|结束`；等 5 分钟再点「继续」→
   从冻结值继续，且落库时长不含这 5 分钟。
5. 倒计时走到 `00:01 → 00:00` → 确认自动完成、常驻通知消失、出现完成通知、记录只写一次。
6. 正向计时跨过 `59:59 → 1:00:00`、持续 1 小时以上，确认格式与耗电。
7. `adb shell am force-stop com.example.yanji` 后确认前台服务与通知的行为，再重启手机复测。

### 已知遗留（不在本次范围）

- `ExamScreen` 仍消费遗留的 `timerState` 镜像，模考路径未重构。
- Activity 被杀后**不会**自动重启前台服务（`START_NOT_STICKY`，原有设计），
  业务层 `ActiveSessionCoordinator` 会恢复会话，但进程重启后计时展示需要重新进入专注页。

---

## 附：最终体验自评

| 目标 | 结果 |
| :--- | :--- |
| 倒计时 = Precision（目标 / 进度 / 秩序） | ✅ 真机确认，细环 + 大数字 + 一行状态 |
| 正向计时 = Flow（空间 / 呼吸 / 持续） | ✅ 真机确认，无圆无环，纯数字 + 极淡呼吸场 |
| 离开 App 仍能看到计时 | ✅ 常驻通知 + 系统 Chronometer（自动走秒） |
| ColorOS 前摄流体云 | ❌ 系统闸门关闭，已按规范降级并给出开启路径 |
| 计时正确性 / 暂停冻结 / 不重复落库 | ✅ 由 `TimerMachine` 保证 + 单测与真机测试守门 |
| 低功耗（无每秒 notify / 无布局动画） | ✅ 通知零每秒刷新，动画全在绘制层 |
| 不破坏标准 Android | ✅ 三级降级，非 OPPO 设备走同一套标准路径 |
